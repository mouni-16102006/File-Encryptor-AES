# File-Encryptor-AES
This project is a Python-based file encryption and decryption tool using AES-256 . It features a secure Command Prompt menu allowing users to generate unique keys, encrypt plain text files into secure ciphertext, and safely reverse the process, showcasing practical implementations of symmetric cryptography.
